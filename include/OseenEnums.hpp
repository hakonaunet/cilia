// NoiseMode.hpp

#pragma once

enum class NoiseMode {
    None,
    Square,
    Circular
};

enum class FrequencyDistribution {
    None,
    Uniform,
    Gaussian
};

enum class AngleDistribution {
    None,
    Uniform,
    Gaussian
};